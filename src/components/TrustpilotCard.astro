---
import { Image } from "astro:assets";
import fivestars from "../assets/images/fivestars.svg";

const { url="#", cardimage="", cardimagealt="", 

navn="", dato="", titel="", review="" } = Astro.props;
---
<article class="bg-blaze-orange-100 py-s px-sm min-w-64 max-w-64 rounded-medium">
    <div class="grid grid-cols-[1fr_4fr] gap-xxs">
        <img src={cardimage} alt={cardimagealt} class="rounded-default">
        <div class="">
            <h2 class="text-xs font-semibold">{navn}</h2>
            <p class="text-[10px]">{dato}</p>
        </div>
    </div>

    <div class="pt-xxs"></div>
<Image src={fivestars} alt="trustpilot" class="" loading="lazy"/>
<p class="font-semibold py-xxs">{titel}</p>
<p>{review}</p>
<a href={url} class="font-semibold text-xs">Læs mere</a>

</article>